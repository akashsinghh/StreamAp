package lambda;
@FunctionalInterface
public interface Myinter {
    public  abstract void hello();
    //so we cant use multiple abstract method in functional interface only on one F.I are use;
    //public abstract void hey();
}
// create a separte a class and implements
